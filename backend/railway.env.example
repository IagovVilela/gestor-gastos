# Variáveis de Ambiente para Railway
# Copie estas variáveis para o painel do Railway

# Database (URL fornecida pelo Railway ao criar o serviço MySQL/PostgreSQL)
DATABASE_URL="mysql://user:password@host:port/database"
# Para PostgreSQL, use:
# DATABASE_URL="postgresql://user:password@host:port/database?schema=public"

# JWT Secrets (GERE VALORES ÚNICOS E SEGUROS!)
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="seu-jwt-secret-super-seguro-aqui"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_SECRET="seu-refresh-secret-super-seguro-aqui"
JWT_REFRESH_EXPIRES_IN="7d"

# Application
NODE_ENV="production"
# PORT é gerenciado automaticamente pelo Railway

# CORS (URL do frontend - configure após deploy do frontend)
FRONTEND_URL="https://seu-frontend.railway.app"



